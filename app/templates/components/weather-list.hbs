{{#if location}}
  <div class="title">
    <h3>{{location.name}}</h3>
  </div>
  <div class="temp">
    <h3>{{weather.tempF}}&deg;</h3>
  </div>
  <div class="icon">
    {{icon-name weather.icon}}
  </div>
{{else}}
  <h1>No saved locations</h1>
{{/if}}

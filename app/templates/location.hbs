<section role="main" id='bg' class='bg'>
  <!-- dont wrap the ui b/c it will hide content. TODO: use z-index instead? -->
</section>

{{typeahead-component transitionToLocation="transitionToLocationHandler"}}

<div class="weather-panel bg">
  <div class="small-12 columns">
    <h1>{{name}}</h1>
    <a {{action 'saveLocation' location}}>Save Location</a>
  </div>
</div>

<div class="weather-stript">
  {{weather-current location=this saveLocationHandler="handleSaveLocation"}}
  <div class="forcast">
    {{weather-forecast forecast=forecast}}
  </div>
</div>

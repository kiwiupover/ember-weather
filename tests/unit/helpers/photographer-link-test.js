import { photographerLink } from 'ember-weather/helpers/photographer-link';
import { module, test } from 'qunit';

module('Unit | Helper | photographer link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = photographerLink([42]);
  assert.ok(result);
});
